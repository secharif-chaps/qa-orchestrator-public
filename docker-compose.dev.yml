services:
  # PostgreSQL Database
  db:
    image: postgres:16-alpine
    restart: always
    env_file:
      - .env.dev
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: mint_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/db/initdb.d:/docker-entrypoint-initdb.d:ro
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # FastAPI Backend
  backend:
    build: 
      context: ./mint-back
    depends_on:
      db:
        condition: service_healthy
    restart: always
    env_file:
      - .env.dev
    ports:
      - "8000:8000"

  # Nuxt Frontend
  frontend:
    build:
      context: ./mint-front
    restart: always
    env_file:
      - .env.dev
    ports:
      - "3000:3000"
    depends_on:
      - backend

  # Keycloak for Authentication
  keycloak:
    image: quay.io/keycloak/keycloak:latest
    restart: unless-stopped
    env_file:
      - .env.dev
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KEYCLOAK_ADMIN_USERNAME}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}
      KEYCLOAK_DB: postgres
      KEYCLOAK_DB_USERNAME: ${KEYCLOAK_DB_USERNAME}
      KEYCLOAK_DB_PASSWORD: ${KEYCLOAK_DB_PASSWORD}
      KEYCLOAK_DB_URL: jdbc:postgresql://db:5432/${KEYCLOAK_DB_NAME}
    command: start-dev --import-realm
    volumes:
      - ./docker/keycloak/realm-mint-dev.json:/opt/keycloak/data/import/realm-mint-dev.json
      - ./docker/keycloak/mint-theme:/opt/keycloak/themes/mint
    ports:
      - "8080:8080"
    depends_on:
      - db

volumes:
  postgres_data: 

  