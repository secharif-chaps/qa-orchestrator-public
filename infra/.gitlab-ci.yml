# ─── Infrastructure CI ─────────────────────────────
# Owned by: team-devops
# Only runs when infra/ files change

.infra-changes: &infra-changes
  changes:
    - infra/**/*

infra:validate:
  stage: lint
  rules:
    - <<: *infra-changes
  script:
    - cd infra && docker compose config --quiet