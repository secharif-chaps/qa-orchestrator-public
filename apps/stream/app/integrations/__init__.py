"""External integration modules for the Stream service.

This package isolates third-party-specific code (NP6, future SMTP fallbacks,
etc.) behind generic seams so the rest of Stream's business logic stays
provider-agnostic. See ADR-0020 §NP6 Integration for the rationale.
"""
