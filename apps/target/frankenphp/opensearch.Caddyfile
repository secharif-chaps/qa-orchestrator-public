# OpenSearch Dashboards reverse proxy
{$OPENSEARCH_SERVER_NAME:opensearch.localhost} {
	reverse_proxy {
		to opensearch-dashboards:5601
		header_up Host {http.request.host}
		header_up X-Forwarded-Port {http.request.port}
	}

	{$CADDY_SERVER_TLS_DIRECTIVES}
}

