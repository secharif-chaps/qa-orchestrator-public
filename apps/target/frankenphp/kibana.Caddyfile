# Kibana reverse proxy
{$KIBANA_SERVER_NAME:kibana.localhost} {
	reverse_proxy {
		to kibana:5601
		header_up Host {http.request.host}
		header_up X-Forwarded-Port {http.request.port}
	}

	{$CADDY_SERVER_TLS_DIRECTIVES}
}
