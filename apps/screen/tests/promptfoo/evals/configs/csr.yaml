# yaml-language-server: $schema=https://promptfoo.dev/config-schema.json
description: "Screen — CSR agent evaluation"

providers: file://../../shared/providers.yaml

prompts:
  - id: file://../providers/loaders.py:load_csr
    label: "CSR Agent"

defaultTest: file://../../shared/base_test.yaml

tests:
  - ../tests/tests_csr.yaml

outputPath: ../results/results_csr.json
