# yaml-language-server: $schema=https://promptfoo.dev/config-schema.json
description: "Screen — Digital agent evaluation"

providers: file://../../shared/providers.yaml

prompts:
  - id: file://../providers/loaders.py:load_digital
    label: "Digital Agent"

defaultTest: file://../../shared/base_test.yaml

tests:
  - ../tests/tests_digital.yaml

outputPath: ../results/results_digital.json
