# yaml-language-server: $schema=https://promptfoo.dev/config-schema.json
description: "Screen — Corporate Structure agent evaluation"

providers: file://../../shared/providers.yaml

prompts:
  - id: file://../providers/loaders.py:load_corporate_structure
    label: "Corporate Structure Agent"

defaultTest: file://../../shared/base_test.yaml

tests:
  - ../tests/tests_corporate_structure.yaml

outputPath: ../results/results_corporate_structure.json
