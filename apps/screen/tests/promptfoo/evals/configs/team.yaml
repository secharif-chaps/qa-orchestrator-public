# yaml-language-server: $schema=https://promptfoo.dev/config-schema.json
description: "Screen — Team agent evaluation"

providers: file://../../shared/providers.yaml

prompts:
  - id: file://../providers/loaders.py:load_team
    label: "Team Agent"

defaultTest: file://../../shared/base_test.yaml

tests:
  - ../tests/tests_team.yaml

outputPath: ../results/results_team.json
