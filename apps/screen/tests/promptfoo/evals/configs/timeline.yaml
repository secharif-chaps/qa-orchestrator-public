# yaml-language-server: $schema=https://promptfoo.dev/config-schema.json
description: "Screen — Timeline agent evaluation"

providers: file://../../shared/providers.yaml

prompts:
  - id: file://../providers/loaders.py:load_timeline
    label: "Timeline Agent"

defaultTest: file://../../shared/base_test.yaml

tests:
  - ../tests/tests_timeline.yaml

outputPath: ../results/results_timeline.json
