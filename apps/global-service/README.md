# ChapsMind Global Service

Shared service for cross-cutting concerns (API gateway, token management, user/organization services).

## Stack

- **Language**: Python
- **Purpose**: Global services shared across all ChapsMind modules

## Commands

```bash
# CI handles lint and build
# Service-specific commands TBD as the service matures
```